import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    color: '#000',
    fontSize: 30,
    marginVertical: 50,
    textAlign: 'center',
  },
});

export default styles;
